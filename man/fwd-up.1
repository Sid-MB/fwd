.\" AUTOGENERATED by tools/generate_man_pages.py; DO NOT EDIT.
.TH "FWD UP" "1" "2026-08-16" "fwdit" "fwd up Manual"
.SH NAME
fwd\-up \- Provision or reuse a target, synchronize the project,
bootstrap its tools, and start a persistent session.
.SH HOMEPAGE
\fIhttps://github.com/Sid-MB/fwd\fP
.SH SYNOPSIS
.B fwd up
[OPTIONS] [selectors]...
.SH DESCRIPTION
Provision or reuse a target, synchronize the project, bootstrap its
tools, and start a persistent session.
.PP
Positionals are [TARGET] [AGENT|COMMAND...]. Magic agents 'claude' and
\&'codex' sync their settings and auto-attach
in an interactive terminal. --reuse attaches to a matching session when
no task command is supplied, or creates one
only in a human terminal. Explicit commands use the same managed task
runner as 'fwd send -- COMMAND' and
stream back with Ctrl-C to cancel and Ctrl-B to background; pass
--attach to enter their session directly, or
--stop-after to stop remote compute after the tracked command. Use '--'
before remote command flags.
.PP
To add a new target, run 'fwd setup'.
.SH OPTIONS
.TP
\fB\-t,\fP \-\-target <str>
Configured target to use; defaults to default_target, or the existing
session's target.
.TP
\fB\-\-agent\fP <str>
Registered coding agent to launch, such as claude or codex.
.TP
\fB\-m,\fP \-\-machine <str>
Exact provider machine string for this launch; inspect valid and
currently available values with --machines.
.TP
\fB\-\-machines\fP
List available and unavailable machine strings, marking each target's
default, then exit.
.TP
\fB\-\-gpu\fP <str>
Override GPU selection for an explicitly GPU-enabled target (RunPod GPU
id or Slurm --gres spec).
.TP
\fB\-n,\fP \-\-name <str>
Session name; defaults to a stable slug derived from this directory.
.TP
\fB\-\-new\fP
Create a fresh session instead of reusing this directory's existing
session. Cannot be combined with --name.
.TP
\fB\-r,\fP \-\-reuse
Reuse a matching session; attach when no task command is supplied, or
create only interactively when none exists.
.TP
\fB\-y,\fP \-\-restart
With --reuse, authorize restarting stopped billable compute without prompting.
.TP
\fB\-a,\fP \-\-attach
Attach directly after startup instead of streaming an explicit command
as a durable task.
.TP
\fB\-\-no\-attach,\fP \-\-detach
Stay local even for magic agent commands that normally auto-attach in a
terminal.
.TP
\fB\-\-session\fP
Move the real transcript so claude resumes it; already the default, pass
this only to re-enable it when config disables it.
.TP
\fB\-\-handoff\fP
Summarize into HANDOFF.md instead of moving the transcript; replaces
--session entirely.
.TP
\fB\-\-user\-config\fP
Upload your ~/.claude bundle (CLAUDE.md, skills, agents, commands,
settings.json); never credentials or history.
.TP
\fB\-\-creds\fP
DANGER: write your live Claude OAuth token to the remote disk; prefer
logging in inside the remote session.
.TP
\fB\-\-setup\-github\fP / \-\-no\-setup\-github
Set up GitHub authentication for this launch; defaults to github.auth
(enabled by default).
.TP
\fB\-\-stop\-after\fP
After an explicit streamed command finishes, stop the remote session
from the server even if this computer disconnects.
.TP
\fB\-\-force\-stop\-after\fP
With --stop-after, permit shutdown even if the remote Git worktree is dirty.
.TP
\fB\-p,\fP \-\-ports <str>
Open PORT or LOCAL:REMOTE after launch; repeat to replace
project-configured defaults for this invocation.
.SH EXIT STATUS
For a foreground explicit command, fwd returns the remote command's exit
status. Other nonzero values indicate selection, provisioning,
synchronization, preparation, launch, or validation failure.
.SH REPORTING BUGS
Report issues at \fIhttps://github.com/Sid-MB/fwd/issues\fP.
.SH SEE ALSO
\fBfwd\fP(1)
